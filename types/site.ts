export interface NavItem {
  label: string;
  href: string;
  isMenu?: boolean;
}

export interface SiteMetadata {
  name: string;
  tagline: string;
  phone: string;
  whatsappNumber: string;
  address: string;
  hours: string;
}
